<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shop_Online">
        <activity
            android:name=".Activity.CartActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.DetailActivity"
            android:exported="false" />
        <!--
            SplashActivity là màn hình chào đầu tiên của ứng dụng (Splash Screen)
            Khi người dùng bấm vào icon ứng dụng, Android sẽ mở Activity này đầu tiên.
        -->
        <activity
            android:name=".Activity.SplashActivity"
            android:exported="true">

            <!--
               android:exported="true"
               → Cho phép hệ thống hoặc ứng dụng khác (như Launcher) gọi tới Activity này.
               Nếu để false thì Launcher (một app khác) không thể khởi chạy ứng dụng.
            -->


            <!--
                intent-filter xác định đây là Activity "chính" (Main Entry Point) của ứng dụng.
                Khi người dùng bấm vào icon app → Android sẽ tìm Activity có intent-filter này để mở.
            -->
            <intent-filter>

                <!--
                action.MAIN → Đánh dấu Activity này là điểm khởi đầu chính của app
                (chỉ nên có duy nhất 1 Activity trong app mang action này)
                -->
                <action android:name="android.intent.action.MAIN" />
                <!--
                       category.LAUNCHER → Cho phép Activity này hiển thị trong danh sách ứng dụng
                       và có icon trên màn hình chính.
                -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
              MainActivity là màn hình chính sau khi qua Splash.
              Vì không phải màn hình khởi động đầu tiên, nên không cần intent-filter.
        -->
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true" />
    </application>

</manifest>